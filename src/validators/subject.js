export const createSubject = {
    name: {
        format: {
            pattern: "[A-z]+",
            flags: "i",
            message: "can only contain a-z "
        }
    }
};

export const editSubject = {
    name: {
        format: {
            pattern: "[A-z]+",
            flags: "i",
            message: "can only contain a-z "
        }
    }
};
