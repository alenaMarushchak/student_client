export const createGroup = {
    name: {
        format: {
            pattern: "[A-z]+",
            flags: "i",
            message: "can only contain a-z "
        }
    }
};

export const editGroup = {
    name: {
        format: {
            pattern: "[A-z]+",
            flags: "i",
            message: "can only contain a-z "
        }
    }
};
