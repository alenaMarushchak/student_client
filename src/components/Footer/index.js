import React from 'react';
import PropTypes from 'prop-types';

function Footer({...props}) {
  return (
    <div>
      header
    </div>
  );
}

Footer.defaultProps = {};

Footer.propTypes = {};

export default Footer;
